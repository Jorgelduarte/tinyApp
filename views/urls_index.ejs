<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <header>
        <% include partials/_header %>
    </header>
<p>A list or table of the URLs and their shortened forms below:</p>
<% for (key in urls) { %>
    <li><%= key %></li>
    <a href="url"> <li><%= urls[key] %></li></a>
    <form method="POST" action="/urls/<%= key %>/edit"><button type="submit">Edit</button></form>
    <form method="POST" action="/urls/<%= key %>/delete"><button type="submit">🗑️</button></form>
<% } %>

</body>
</html>
